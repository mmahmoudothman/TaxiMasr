<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.osos.taximasr">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoooo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".driver.DriverLogin" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".driver.DriverCommunicateAcivity" />
        <activity android:name=".driver.DriverCostActivity" />
        <activity android:name=".customer.Customer_registeration" />
        <activity android:name=".customer.Review" />
        <activity
            android:name=".customer.Customer_maps"
            android:label="@string/title_activity_customer_maps" />
        <activity android:name=".customer.CustomerBooking" />
        <activity
            android:name=".customer.PostCode"
            android:label="@string/title_activity_post_code"
            android:theme="@style/AppTheme" />
        <activity android:name=".customer.CustomerOrder" />
        <activity
            android:name=".customer.CustomerMeshwoark"
            android:label="@string/title_activity_meshwoark" />
        <activity
            android:name=".customer.Meshwoark2"
            android:label="@string/title_activity_meshwoark2" />
        <activity
            android:name=".customer.FindDriver"
            android:label="@string/title_activity_find_driver" />
        <activity android:name=".driver.DriverCommunicateWithCustomer" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customer.CallDriver" />
        <activity android:name=".customer.Customer_Drawer"></activity>
    </application>

</manifest>